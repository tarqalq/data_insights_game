# 🎮 موقع اللعبة التفاعلية - مجتمع رؤى البيانات

منصة لعبة تفاعلية متعددة اللاعبين مصممة لفعاليات مجتمع رؤى البيانات، تعتمد على نظام الأسئلة والأجوبة مع وجود "جواسيس" يحاولون الاندماج.

## ✨ الميزات الرئيسية

- 🎯 **لعبة تفاعلية** مع نظام جواسيس ذكي
- 👥 **متعدد اللاعبين** باستخدام Socket.IO للتواصل الفوري
- 🎨 **وضعان للعرض**: فاتح وداكن مع تبديل سلس
- 📊 **لوحة متصدرين** تحفظ النقاط في قاعدة بيانات PostgreSQL
- 🎭 **شاشة عرض منفصلة** للجمهور
- 👑 **لوحة تحكم آدمن** شاملة
- 🔔 **نظام إشعارات** Toast للتغذية الراجعة الفورية
- 🌐 **دعم كامل للغة العربية** مع RTL

## 🚀 البدء السريع

### المتطلبات الأساسية

- Node.js (الإصدار 14 أو أحدث)
- PostgreSQL (الإصدار 12 أو أحدث)
- npm أو yarn

### خطوات التثبيت

1. **استنساخ المشروع**
   ```bash
   git clone <repository-url>
   cd "GAME WEB - نسخة (2)"
   ```

2. **تثبيت الحزم**
   ```bash
   npm install
   ```

3. **إعداد قاعدة البيانات**
   
   أ. إنشاء قاعدة بيانات PostgreSQL:
   ```bash
   psql -U postgres
   CREATE DATABASE data_insights_db;
   \q
   ```
   
   ب. تهيئة الجداول:
   ```bash
   npm run db:init
   ```

4. **إعداد ملف البيئة**
   
   انسخ ملف `.env.example` إلى `.env` وعدّل القيم:
   ```bash
   cp .env.example .env
   ```
   
   ثم عدّل الملف بمعلومات قاعدة البيانات الخاصة بك:
   ```env
   DB_USER=postgres
   DB_HOST=localhost
   DB_NAME=data_insights_db
   DB_PASSWORD=your_password_here
   DB_PORT=5432
   PORT=3000
   ```

5. **تشغيل المشروع**
   
   للتطوير (مع إعادة التشغيل التلقائي):
   ```bash
   npm run dev
   ```
   
   للإنتاج:
   ```bash
   npm start
   ```

6. **فتح الموقع**
   - الصفحة الرئيسية: http://localhost:3000
   - لوحة الآدمن: http://localhost:3000/admin
   - شاشة العرض: http://localhost:3000/display

## 📁 هيكل المشروع

```
├── app.js                 # ملف السيرفر الرئيسي
├── package.json           # معلومات المشروع والحزم
├── .env                   # متغيرات البيئة (لا يُرفع على Git)
├── .env.example           # نموذج لملف البيئة
├── .gitignore             # ملفات يتم تجاهلها من Git
│
├── database/              # ملفات قاعدة البيانات
│   ├── schema.sql         # هيكل قاعدة البيانات
│   └── init.js            # سكريبت التهيئة
│
├── public/                # الملفات الثابتة
│   ├── css/
│   │   ├── style.css      # الستايلات الرئيسية
│   │   └── themes.css     # نظام الثيمات
│   ├── js/
│   │   ├── theme-switcher.js    # سكريبت تبديل الثيم
│   │   ├── utils.js             # دوال مساعدة
│   │   └── notifications.js     # نظام الإشعارات
│   └── [صور]
│
└── views/                 # قوالب EJS
    ├── partials/
    │   ├── header.ejs
    │   └── footer.ejs
    ├── index.ejs          # الصفحة الرئيسية
    ├── lobby.ejs          # صفحة الانتظار
    ├── game.ejs           # صفحة اللعبة
    ├── results.ejs        # صفحة النتائج
    ├── leaderboard.ejs    # لوحة المتصدرين
    ├── admin.ejs          # لوحة الآدمن
    └── display.ejs        # شاشة العرض
```

## 🎮 كيفية اللعب

### للاعبين

1. افتح الموقع وأدخل اسمك
2. انتظر في اللوبي حتى يبدأ المنظم اللعبة
3. ستحصل على سؤال (قد تكون مواطناً أو جاسوساً)
4. أجب على السؤال بصدق
5. صوّت على من تظن أنهم الجواسيس
6. شاهد النتائج واكسب النقاط!

### للمنظم (الآدمن)

1. افتح `/admin`
2. اختر رقم الجولة
3. اضغط "إطلاق الأسئلة المزدوجة"
4. انتظر إجابات اللاعبين
5. اضغط "كشف الإجابات للتصويت"
6. اضغط "كشف الجواسيس والنتائج"
7. اضغط "عرض لوحة المتصدرين"

## 🎨 نظام الثيمات

الموقع يدعم وضعين:
- 🌙 **الوضع الداكن** (افتراضي)
- ☀️ **الوضع الفاتح**

للتبديل:
- اضغط على الزر العائم في أعلى اليسار
- أو استخدم الاختصار: `Ctrl/Cmd + Shift + T`

## 🔧 التحسينات الأخيرة

### الأمان
- ✅ نقل بيانات قاعدة البيانات إلى ملف `.env`
- ✅ إضافة `.gitignore` لحماية الملفات الحساسة
- ✅ تحسين معالجة الأخطاء

### التصميم
- ✅ نظام ثيمات كامل (فاتح/داكن)
- ✅ تحسين الألوان والتباين
- ✅ نظام إشعارات Toast
- ✅ تحسين تجربة المستخدم

### الأداء
- ✅ إنشاء ملف دوال مساعدة مشتركة
- ✅ تحسين خوارزميات الترتيب
- ✅ إضافة فهارس لقاعدة البيانات

## 📝 السكريبتات المتاحة

```bash
npm start        # تشغيل السيرفر
npm run dev      # تشغيل السيرفر مع إعادة التشغيل التلقائي
npm run db:init  # تهيئة قاعدة البيانات
```

## 🐛 حل المشاكل الشائعة

### خطأ في الاتصال بقاعدة البيانات

تأكد من:
1. تشغيل PostgreSQL
2. صحة بيانات الاتصال في ملف `.env`
3. وجود قاعدة البيانات المحددة

### الصفحة لا تُحمّل

تأكد من:
1. تشغيل السيرفر (`npm start` أو `npm run dev`)
2. عدم استخدام المنفذ 3000 من تطبيق آخر
3. فتح الرابط الصحيح: http://localhost:3000

### الثيم لا يتبدل

تأكد من:
1. تحميل ملف `theme-switcher.js` بشكل صحيح
2. عدم وجود أخطاء في console المتصفح
3. تفعيل JavaScript في المتصفح

## 🤝 المساهمة

هذا المشروع مفتوح للتحسينات! إذا كان لديك اقتراح:

1. Fork المشروع
2. أنشئ فرع للميزة (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push للفرع (`git push origin feature/AmazingFeature`)
5. افتح Pull Request

## 📄 الترخيص

هذا المشروع مطور بكل حب لدعم فعاليات مجتمع رؤى البيانات.

## 📞 التواصل

لجنة الفعاليات - مجتمع رؤى البيانات (Data Insights)

---

**ملاحظة**: تأكد من عدم رفع ملف `.env` على Git لحماية بيانات قاعدة البيانات الخاصة بك!
